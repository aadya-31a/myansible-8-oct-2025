--- 
- hosts: webserver
  become: yes 
  tasks: 
   - name: install java on ubuntu
     apt: 
       name: "{{myjavspackonubuntu}}"
       state: present 
     when: ansible_os_family=="Debian"

   - name: install java on Redhat 
     dnf:
       name: "{{myjavspackonubuntu}}"
       state: present 
     when: ansible_os_family=="Redhat" 
